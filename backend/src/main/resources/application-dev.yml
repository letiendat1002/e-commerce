spring:
  datasource:
    url: jdbc:mysql://localhost:3333/myecommerce
    username: root
    password: root

logging:
  level:
    org:
      springframework:
        web: DEBUG
      hibernate: ERROR
