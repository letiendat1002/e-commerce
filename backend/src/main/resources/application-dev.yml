spring:
  datasource:
    url: jdbc:mysql://localhost:3333/myecommerce
    username: root
    password: root

env-variable:
  url: http://localhost:8080

logging:
  level:
    org:
      springframework:
        web: DEBUG
      hibernate: ERROR
